# Title

Title

## Usage

``` r
loading(pos = NULL, weights = NULL)
```

## Arguments

- pos:

  defines the position of each one of the elements of the block of
  states defined. `NULL` indicates by default the first state included
  in the block (pos=0)

- weights:

  defines the weights associated to each one of the state variables
  included in the block.
