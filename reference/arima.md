# Autoregressive Integrated Moving Average (ARIMA) Model

Autoregressive Integrated Moving Average (ARIMA) Model

## Usage

``` r
arima(name, ar, diff, ma, var = 1, fixed = FALSE)
```

## Arguments

- fixed:
